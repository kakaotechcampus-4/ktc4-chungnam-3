# 기획 변경 검토: 위치 기반 알림에서 방문 코스 생성으로

> 상태: 기획 초안  
> 본 문서는 기존 기획에서 에이전트의 필요성이 부족했던 이유와 이를 보완하기 위한 변경 방향을 정리한다.

## 1. 기존 기획의 한계

기존 기획의 핵심은 사용자가 저장한 콘텐츠의 장소 근처에 도착하면 해당 콘텐츠를 다시 보여주는 것이었다.

그러나 이 기능은 다음과 같은 규칙으로 구현할 수 있다.

```text
현재 위치 수신
→ 저장된 장소와 거리 계산
→ 설정된 반경 안에 있으면 알림 발송
```

알림 중복 방지, 쿨다운, 시간대 제한이 추가되더라도 명시적인 조건문과 지오펜싱으로 처리할 수 있다.

영상에서 장소를 추출하기 위해 AI를 사용할 수는 있지만, 이는 비정형 데이터를 구조화하는 모델 사용이다. 시스템이 스스로 목표 달성을 위한 계획을 세우거나 결과에 따라 행동을 변경하는 것은 아니므로, 에이전트가 반드시 필요한 과업이라고 보기 어렵다.

## 2. 변경할 핵심 사용자 과업

프로젝트의 핵심 과업을 단순한 위치 기반 알림에서 다음과 같이 변경한다.

> 사용자가 저장한 콘텐츠와 공용 콘텐츠를 활용하여, 현재 조건에서 실제로 방문할 수 있는 코스를 구성한다.

사용자는 채팅 대신 화면에서 다음 조건을 선택한다.

- 출발 위치 또는 방문 지역
    
- 사용 가능한 시간
    
- 이동 수단
    
- 원하는 활동이나 장소 유형
    
- 개인 저장물 우선 여부
    

시스템은 조건에 맞는 장소를 나열하는 데서 끝나지 않고, 운영 상태와 이동시간까지 확인하여 실행 가능한 방문 순서를 반환한다.

## 3. 콘텐츠 탐색 범위

코스 에이전트는 사용자가 직접 저장한 콘텐츠를 우선 탐색한다.

개인 저장물만으로 코스를 완성할 수 없다면, 공유에 동의한 콘텐츠로 구성된 공용 풀까지 검색 범위를 확장한다.

결과에서는 출처를 다음과 같이 구분한다.

- `내 저장물`: 사용자가 직접 저장한 콘텐츠
    
- `추천 장소`: 공용 풀에서 보완한 콘텐츠
    

콘텐츠 분석 결과는 동일한 영상에 대해 한 번만 생성하여 재사용한다. 사용자가 어떤 콘텐츠를 저장했는지는 별도의 개인 데이터로 관리하며, 다른 사용자에게 저장한 사람의 식별 정보나 저장 이력은 노출하지 않는다.

공용 풀의 동의 방식과 개인정보 처리 기준은 별도의 정책 검토를 거쳐 확정한다.

## 4. 코스 에이전트의 목표

코스 에이전트의 목표는 추천 점수가 높은 장소를 고르는 것이 아니라 다음 조건을 만족하는 방문 계획을 완성하는 것이다.

- 사용자가 선택한 시간 안에 방문할 수 있음
    
- 현재 이용할 수 있는 장소로 구성됨
    
- 장소 사이의 이동이 현실적으로 가능함
    
- 사용자가 원한 활동과 코스의 구성이 의미적으로 어울림
    
- 가능하면 사용자가 직접 저장한 콘텐츠를 포함함
    

시간 제한, 폐업 여부와 같은 하드 제약은 코드로 검증한다.

장소 분위기, 활동의 조합, 개인 저장물과 추천 장소의 비율과 같은 소프트 목표는 에이전트가 비교하고 조정한다.

## 5. 에이전트가 사용하는 도구

초기에는 다음과 같은 최소 도구를 제공한다.

- 개인 저장 콘텐츠 검색
    
- 공용 콘텐츠 검색
    
- 장소 운영 상태 및 영업시간 조회
    
- 장소 간 이동시간 및 경로 조회
    

에이전트는 모든 후보를 무조건 검증하지 않는다. 현재 계획에 유망한 후보부터 확인하고, 관찰한 결과에 따라 추가 도구 호출이 필요한지 판단한다.

## 6. 계획 수립과 복구

에이전트는 후보를 선택한 뒤 외부 도구를 통해 실제 방문 가능성을 확인한다.

도구 결과가 기존 계획을 무효화하면 실패 원인에 따라 복구 방법을 선택한다.

- 장소가 폐업했거나 이용할 수 없으면 같은 역할의 다른 후보로 교체한다.
    
- 전체 시간이 초과되면 장소를 제거하거나 더 가까운 후보로 변경한다.
    
- 이동 경로가 비효율적이면 방문 순서를 변경한다.
    
- 개인 저장물만으로 코스를 만들 수 없으면 공용 풀까지 탐색한다.
    
- 후보가 부족하면 검색 범위를 넓히거나 소프트 조건을 일부 완화한다.
    
- 하드 제약을 만족할 방법이 없으면 코스를 억지로 만들지 않고 생성 불가로 종료한다.
    

에이전트는 사용자의 하드 제약을 임의로 완화할 수 없다.

## 7. 동작 예시

사용자가 성수에서 두 시간 동안 카페와 전시 공간을 방문하는 코스를 요청했다고 가정한다.

에이전트는 먼저 개인 저장물에서 카페와 전시 공간을 탐색한다. 카페만 발견되면 공용 풀에서 전시 공간 후보를 찾는다.

이후 각 장소의 운영 상태와 이동시간을 확인한다. 선택한 전시가 종료되었다면 다른 전시 후보로 교체한다. 두 장소를 방문하면 제한 시간을 초과한다면 더 가까운 장소를 찾거나 방문 순서를 변경한다.

검증 결과 두 시간 안에 실행 가능한 조합이 완성되면 코스를 반환한다. 정해진 탐색 예산 안에서 조건을 만족하는 조합을 찾지 못하면 생성 불가로 종료한다.

## 8. 에이전트가 필요한 이유

장소 간 거리와 영업시간만으로 최적 경로를 계산하는 문제라면 일반적인 검색 및 경로 최적화 알고리즘으로 해결하는 것이 적절하다.

에이전트가 필요한 부분은 불완전한 후보에서 시작하여 다음 행동을 반복적으로 선택하는 과정이다.

```text
후보 탐색
→ 계획 구성
→ 현실 제약 검증
→ 실패 원인 관찰
→ 복구 전략 선택
→ 계획 수정
```

어떤 후보를 먼저 확인할지, 추가 탐색이 필요한지, 실패한 장소를 교체할지, 코스 전체를 다시 구성할지는 중간 결과와 소프트 목표에 따라 달라진다.

따라서 에이전트의 역할은 경로를 직접 계산하는 것이 아니라, 제한된 도구 호출 예산 안에서 필요한 정보를 수집하고 계획과 복구 전략을 선택하는 것이다.

## 9. Spring 서버와 에이전트의 책임

Spring 서버는 다음과 같은 결정적이고 통제 가능한 작업을 담당한다.

- 사용자 권한 및 데이터 접근 범위 확인
    
- 콘텐츠와 사용자 저장 관계 관리
    
- 외부 API 실제 호출
    
- 하드 제약 검증
    
- 도구 호출 횟수와 전체 실행시간 제한
    
- 최종 결과 저장
    

코스 에이전트는 다음 작업을 담당한다.

- 검색 범위 결정
    
- 후보 비교 및 조합
    
- 필요한 도구 선택
    
- 도구 결과 해석
    
- 실패 원인에 따른 복구 전략 선택
    
- 코스 완성 또는 생성 불가 결정
    

에이전트는 DB를 직접 수정하거나 서버가 정한 제한을 변경할 수 없다.

## 10. 종료 조건

에이전트 실행은 다음 중 하나에 해당하면 종료한다.

- 모든 하드 제약을 만족하는 코스가 완성된 경우
    
- 더 이상 사용할 수 있는 복구 방법이 없는 경우
    
- 최대 도구 호출 횟수에 도달한 경우
    
- 전체 실행 제한시간을 초과한 경우
    

구체적인 호출 횟수와 제한시간은 실제 도구의 응답시간과 비용을 측정한 후 결정한다.

## 11. 기능별 구현 방식

- 콘텐츠 저장 및 영상 분석: 정해진 절차의 파이프라인
    
- 단순 위치 기반 알림: 지오펜싱과 규칙
    
- 경로 및 이동시간 계산: 결정적 알고리즘과 외부 API
    
- 콘텐츠 탐색, 장소 조합, 검증 결과에 따른 계획 복구: 코스 에이전트
    

이 구분을 통해 규칙과 알고리즘으로 해결할 수 있는 영역에 LLM을 사용하지 않고, 결과를 예측하기 어려운 탐색과 계획 수정에만 에이전트를 사용한다.

## 12. 성립 조건

코스 후보를 한 번 생성한 뒤 그대로 반환한다면 이는 에이전트가 아니라 LLM 기반 추천 기능에 가깝다.

따라서 변경된 기획이 에이전트 시스템으로 성립하려면 다음 기능이 실제로 구현되어야 한다.

- 모델이 직접 검색 및 검증 도구를 선택함
    
- 도구 결과가 기존 계획을 변경할 수 있음
    
- 실패 원인에 따라 서로 다른 복구 행동을 선택함
    
- 복구 후 변경된 계획을 다시 검증함
    
- 완료 조건이나 실행 제한에 도달할 때까지 반복함